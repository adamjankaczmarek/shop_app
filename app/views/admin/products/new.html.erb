<%= form_for @product, :url => { :action => "create" }, do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :price %>
  <%= f.select(:category, @categories)%>
  <%= f.submit "Create" %>
<% end %>