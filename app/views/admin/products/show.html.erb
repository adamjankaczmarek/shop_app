<%= form_for @product, :url => { :action => "update" }, do |f| %>
  <%= f.text_field :name %>
  <%= f.text_field :price %>
  <%= f.select(:category, @categories)%>
  <%= f.submit "Update" %>
<% end %>

<%= link_to "Delete", admin_product_path(@product), :action => "destroy", :method => :delete %>
